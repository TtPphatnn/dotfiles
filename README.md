# dotfiles
my first rice for arch linux
It's not much but enough for my basic usage

I use vscodium for easier editing config and since i do not have time to config the nvim.

dependencies:
hyprland - wayland compositor
cava - audio visualizer
waybar-cava-git - the status bar with cava integration.
kitty - just a default terminal

